// 산술 연산자
var area = 5 * 4;   // 20

// 문자열 연결 연산자
var str = 'My Name Is ' + 'Lee';    //My Name Is Lee

// 할당 연산자
var color = 'red';

// 비교 연산자
var foo = 3 > 5;    // false

// 논리 연산자
var bar = (5 > 3) && (2 < 4);   //true

// 타입 연산자
var type = typeof 'Hi';   //string

// 인스턴스 생성 연산자
var today = new Data(); // 현재 날짜와 시간을 알려준다.

// 자바스크립트는 암묵적 타입 강제 변환을 통해 연산을 수행한다.
var foo = 1 + '10';   //'110'
var foo = 1 8 '10';   //'10'
