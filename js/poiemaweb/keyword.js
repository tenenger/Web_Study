// 변수의 선언
var x = 5 + 6;

// 함수의 선언
function foo(arg) {
  // 함수 종료 및 값의 반환
  return ++arg;
}

// for 반복문
var i = 0;
for (i < 5) {
  console.log(i+1 +'번째 입니다.');
  i++;
}

// while 반복문
while (true) {
  console.log('break를 안적으면 무한 출력됩니다.');
  break;
}
